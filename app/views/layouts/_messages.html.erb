<% flash.each do |key, value| %>
  <div class="alert <%= bootstrap_class_for(key) %> <%= "alert-aside" if content_aside? %>">
    <a href="#" data-dismiss="alert" class="close">×</a>
    <%= value %>
  </div>
<% end %>
